# GeoShock v2 — requirements
# Install: pip install -r requirements.txt

# ── Numerics & data ───────────────────────────────────────────────────────────
numpy>=1.24
pandas>=2.0
scipy>=1.10

# ── Data fetching ─────────────────────────────────────────────────────────────
fredapi>=0.5          # FRED API (free key: https://fred.stlouisfed.org/docs/api/api_key.html)
yfinance>=0.2.28      # Yahoo Finance (no key)
requests>=2.28        # GDELT + EIA + FAO direct downloads
openpyxl>=3.1         # .xlsx parsing (GSCPI, GPR index)
xlrd>=2.0             # .xls parsing (FAO food price index legacy format)
python-dotenv>=1.0    # .env file loading

# ── Econometrics & ML ─────────────────────────────────────────────────────────
statsmodels>=0.14     # QuantReg, VAR
scikit-learn>=1.3     # PCA, QuantileRegressor, StandardScaler

# ── Layer 0 — LLM CAMEO coding ───────────────────────────────────────────────
anthropic>=0.25       # Claude API (optional — falls back to rule-based if absent)

# ── Visualisation ─────────────────────────────────────────────────────────────
matplotlib>=3.7
plotly>=5.18,<6.0     # pin below 6.0 — avoids st.plotly_chart config deprecation
seaborn>=0.12

# ── Dashboard ─────────────────────────────────────────────────────────────────
streamlit>=1.34,<2.0  # pin below 2.0 for plotly compatibility

# ── I/O ───────────────────────────────────────────────────────────────────────
pyarrow>=14.0         # Parquet I/O
